---
title: Hello World
date: 2026-04-20
summary: First post — verifying that the site renders math, code, and prose correctly.
---

# Hello World

This is a placeholder post to verify the site template renders correctly. If you can read this, the Markdown-to-HTML pipeline is working.

## Math rendering

Einstein's mass–energy equivalence, inline: $E = mc^2$.

And a display equation — the Gaussian integral:

$$\int_{-\infty}^{\infty} e^{-x^2} \, dx = \sqrt{\pi}$$

## Code rendering

A short Python snippet to confirm code blocks work:

```python
def fibonacci(n):
    a, b = 0, 1
    for _ in range(n):
        a, b = b, a + b
    return a

print(fibonacci(10))  # 55
```

That's it. Real content coming soon.
